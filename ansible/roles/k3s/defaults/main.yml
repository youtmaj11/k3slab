---
# Default values for the k3s bootstrap role
k3s_dependencies:
  - curl
  - ca-certificates
  - apt-transport-https
  - gnupg
  - software-properties-common

k3s_install_url: https://get.k3s.io
k3s_channel: stable
k3s_exec_args: "--write-kubeconfig-mode 644"
