apiVersion: v1
kind: Service
metadata:
  name: kyverno-sentinel
  namespace: kyverno-sentinel
  labels:
    app: kyverno-sentinel
spec:
  selector:
    app: kyverno-sentinel
  ports:
    - name: metrics
      port: 8000
      targetPort: 8000
