# Application Skeleton Configuration

# Database Configuration
DB_HOST=postgres.default.svc.cluster.local
DB_PORT=5432
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION

# Redis Configuration
REDIS_HOST=redis.default.svc.cluster.local
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_IN_PRODUCTION

# Application
APP_ENV=development
APP_DEBUG=false
LOG_LEVEL=info

# NOTE: This is a template file. Copy to .env and update with actual values.
# NEVER commit the .env file to version control.
